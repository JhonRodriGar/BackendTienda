
public enum PropertiesParaMysql {
	
	/*
	 * Con los siguientes datos se actualiza el archivo application.properties para que el programa se conecte a la 
	 * base de datos de MySQL
	 */

	/*
	spring.datasource.url = jdbc:mysql://localhost:3306/crud?useSSL=false&serverTimezone=UTC&useLegacyDateTimeCode=false

	# nombre de usuario y contrasena
	spring.datasource.username = root
	spring.datasource.password = 

	# mostrar sentencias SQL en la consola
	spring.jpa.show-sql = true

	# actualizar base de datos y crear entidades
	#spring.jpa.hibernate.ddl-auto = update

	# hibernate genera SQL optimizado
	spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

	#Por defecto corría por el puerto 8080 pero me aparecía en uso entonces le asigné el 80881
	server.port = 8081

	# security. jwt.secret va a ser la variable a utilizar para la fima de seguridad. jwt.expiration es el tiempo de expiración, 36000 minutos son 10 horas
	jwt.secret = secret 
	jwt.expiration = 36000
	*/
	
}
